import 'package:flutter/material.dart';

class EditInventoryProducts extends StatelessWidget {
  const EditInventoryProducts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Edit products'),
    );
  }
}