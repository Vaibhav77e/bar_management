import 'package:flutter/material.dart';

class CreateNewStaffs extends StatelessWidget {
  const CreateNewStaffs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Staffs page'),
    );
  }
}